#version 460

#include "Size.glsl"

layout(local_size_x = 1, local_size_y = 1, local_size_z = 1) in;

layout(std430, binding = SBUF_DRAW_ARGS_BINDING) writeonly buffer bDrawArgs { uvec4 gDrawArgs[]; };

void main() { gDrawArgs[0] = uvec4(4, 0, 0, 0); }
